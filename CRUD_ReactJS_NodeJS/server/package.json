{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf ./dist/*",
    "build": "npm run clean && npm install && npm run type",
    "start": "node -r ts-node/register ./src/server.ts",
    "dev": "ts-node-dev 'src/server.ts'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "mysql2": "^2.3.3",
    "nodemon": "^2.0.16"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "ts-node": "^10.8.1"
  }
}
